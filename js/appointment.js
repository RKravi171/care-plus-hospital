const dateInput=document.querySelector('input[name="date"]');dateInput.min=new Date().toISOString().split('T')[0];
document.getElementById('appointmentForm').addEventListener('submit',e=>{e.preventDefault();const data=Object.fromEntries(new FormData(e.target).entries());data.id='CP-'+Math.floor(100000+Math.random()*900000);localStorage.setItem('carePlusAppointment',JSON.stringify(data));const box=document.getElementById('appointmentResult');box.classList.remove('hidden');box.innerHTML=`<h2>✓ Appointment Confirmed</h2><p>Your appointment has been booked successfully.</p><p><b>Appointment ID:</b> ${data.id}<br><b>Patient:</b> ${data.patient}<br><b>Doctor:</b> ${data.doctor}<br><b>Date:</b> ${data.date}<br><b>Time:</b> ${data.time}</p><a class="btn btn-primary" href="portal.html">Open Patient Portal →</a>`;window.scrollTo({top:box.offsetTop-100,behavior:'smooth'});});
