const box=document.getElementById('portalContent');const a=JSON.parse(localStorage.getItem('carePlusAppointment')||'null');if(!a){box.innerHTML=`<div class="info-panel" style="text-align:center"><div class="round-icon" style="margin:auto">▣</div><h2>No Appointment Yet</h2><p style="color:var(--muted)">You have not booked an appointment. Create one to see it here.</p><a class="btn btn-primary" href="appointment.html">Book Appointment →</a></div>`}else{box.innerHTML=`<div class="portal-grid"><div class="portal-card"><span>Patient</span><strong>${a.patient}</strong></div><div class="portal-card"><span>Appointment ID</span><strong>${a.id}</strong></div><div class="portal-card"><span>Status</span><strong style="color:var(--teal)">Confirmed</strong></div><div class="portal-card appointment-view"><p class="eyebrow">UPCOMING APPOINTMENT</p><h2>${a.doctor}</h2><p style="color:var(--teal)">${a.department}</p><div class="form-grid"><p><b>Date</b><br>${a.date}</p><p><b>Time</b><br>${a.time}</p><p><b>Phone</b><br>${a.phone}</p><p><b>Reason</b><br>${a.reason||'General consultation'}</p></div><button class="btn btn-outline" onclick="cancelAppointment()">Cancel Appointment</button></div></div>`}function cancelAppointment(){localStorage.removeItem('carePlusAppointment');location.reload()}
